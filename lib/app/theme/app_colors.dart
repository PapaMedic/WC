import 'package:flutter/material.dart';

class AppColors {
  // ── Backgrounds ──────────────────────────────────────────────────
  static const Color scaffoldBackground = Color(0xFF0D0F0D); // near-black
  static const Color sidebarBackground = Color(0xFF111511); // dark forest
  static const Color cardBackground = Color(0xFF1C201C);    // dark charcoal-olive

  // ── Accents ───────────────────────────────────────────────────────
  static const Color primaryAccent = Color(0xFFFF5A00);     // fire orange
  static const Color secondaryAccent = Color(0xFF6B8E23);   // muted olive

  // ── Text ─────────────────────────────────────────────────────────
  static const Color textPrimary = Color(0xFFEADDCA);       // sand/off-white
  static const Color textMuted = Color(0xFF8F9779);         // grey-green

  // ── Borders ───────────────────────────────────────────────────────
  static const Color border = Color(0xFF2A2F2A);            // subtle dark-olive border
  static const Color borderOlive = Color(0xFF4A5E23);       // stronger olive border

  // ── Status ────────────────────────────────────────────────────────
  static const Color statusGreen = Color(0xFF4CAF50);
  static const Color statusRed = Color(0xFFEF5350);
  static const Color statusAmber = Color(0xFFFFA726);
}
